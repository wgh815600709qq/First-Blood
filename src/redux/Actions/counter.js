import {INCREASE_COUNTER, RESET_COUNTER, GET_COUNTER} from './actionTypes.js'
export const increaseCounter = {
  type: INCREASE_COUNTER
}

export const resetCounter = {
  type: RESET_COUNTER
}


export const getCounter = {
  type: GET_COUNTER
}
